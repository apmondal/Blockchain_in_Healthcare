import { Typography } from '@mui/material';
import React from 'react';

const PageNotFound = () => {
  return (
    <Typography variant="h1" color="teal">
      Sorry!!404 PageNotFound!!
    </Typography>
  );
};

export default PageNotFound;
